{% macro default(name, width, height) %}
    <svg width="{{ width }}" height="{{ height }}" viewBox="0 0 {{ width }} {{ height }}" aria-hidden="true" focusable="false">
        <use href="#icon-{{ name }}" />
    </svg>
{% endmacro %}