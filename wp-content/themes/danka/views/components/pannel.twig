{% macro default(data) %}

    <div id="{{ data.id }}" class="component-pannel {% if data.direction %}component-pannel--{{ data.direction }}{% endif %}" data-lenis-prevent>
        <div class="component-pannel__content">
            {{ data.html }}
        </div>
    </div>
{% endmacro %}